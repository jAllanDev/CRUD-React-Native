const MathUtils = {
    somar: (a, b) => parseInt(a) + parseInt(b),
    subtrair: (a, b) => parseInt(a) - parseInt(b),
    multiplicar: (a, b) => parseInt(a) * parseInt(b),
    dividir: (a, b) => parseInt(a) / parseInt(b),
  };
  export default MathUtils;